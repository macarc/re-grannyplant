{
  "name": "simple-grannyplant",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve": "firebase serve",
    "build": "esbuild src/main.js --bundle --minify --outdir=public",
    "dev": "esbuild src/main.js --bundle --watch --outdir=public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase": "^9.0.1-2021730202621",
    "snabbdom": "^3.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.12.25",
    "prettier": "^2.3.2"
  }
}
